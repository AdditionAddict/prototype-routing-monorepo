# PrototypeRoutingMonorepo

Prototype to test routing in a monorepo.

Run `npm run start:login-app`. Navigate to `http://localhost:4000/`.

Run `npm run start:feature-one`. Navigate to `http://localhost:4100/`.